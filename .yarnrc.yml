# PnP breaks jest, React tooling and native hunspell — do not try it.
nodeLinker: node-modules

# Migrated from the deleted .npmrc (registry=… / access=public).
npmRegistryServer: "https://registry.npmjs.org"
npmPublishAccess: public
