import { DerivedLemma } from './types';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function deriveFromPronoun(
  _lemma: string,
  _xpos: string,
): DerivedLemma[] {
  return [];
}
